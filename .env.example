# Local personal mode is the default. Do not put secrets or financial data here.
OLOS_DEMO_MODE=false

# Hosted judging only: set true and use an ephemeral directory.
# OLOS_DEMO_MODE=true
# OLOS_DEMO_DATA_DIR=/tmp/olos-budget-demo
